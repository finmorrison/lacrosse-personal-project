create table lax_site_items
(
itemID SERIAL PRIMARY KEY,
itemName VARCHAR(100),
itemPrice INTEGER,
pocketPlace TEXT,
meshType text,
meshColor text,
sidewallColor text,
shootingStringColor text,
specialtyStyle text,
headtype text
)